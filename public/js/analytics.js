
(function() {
    // Analytics disabled due to persistent 400 errors from tganalytics.xyz
    // The analytics service token appears to be invalid/expired
    // Re-registration with the service is required to fix this issue
    // Analytics is non-critical to app functionality, so it's safely disabled
    console.log('[Analytics] Disabled - TGAnalytics service returning 400 errors');
    return;
})();
